# Design tokens

Tokens de cor da marca Placarium em formatos reutilizaveis.

- `colors.json`: fonte estruturada para scripts, Tailwind ou Style Dictionary.
- `colors.css`: variaveis CSS prontas para prototipos e app web.

O tema claro fica em `:root`; o dark mode usa `[data-theme="dark"]`.

