{
  "name": "@placarium/db",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "db:generate": "echo 'SPEC-003 pendente: drizzle-kit generate' && exit 1",
    "db:migrate": "echo 'SPEC-003 pendente: drizzle-kit migrate' && exit 1",
    "db:seed": "echo 'SPEC-003 pendente: seed' && exit 1",
    "db:studio": "echo 'SPEC-003 pendente: drizzle-kit studio' && exit 1"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.5.0",
    "vitest": "^3.0.0"
  }
}
