pre-commit:
  commands:
    biome:
      glob: "*.{ts,tsx,js,mjs,json,css}"
      run: pnpm exec biome check --no-errors-on-unmatched {staged_files}
pre-push:
  commands:
    typecheck:
      run: pnpm typecheck
    test:
      run: pnpm test
